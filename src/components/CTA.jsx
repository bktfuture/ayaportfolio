import { Link } from 'react-router-dom';

const CTA = () => {
	return (
		<section className="cta flex">
			<p className="q-text text-center">
				“The purpose of computing is insight, not numbers.”
				<br className="sm:block hidden" /> <span className="text-xl">Richard W. Hamming</span>
			</p>
		</section>
	);
};

export default CTA;
